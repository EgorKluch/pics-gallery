div#loginForm(ng-controller='SignInCtrl')
  if !user
    form(name='loginForm', novalidate): table
      tr
        td Логин:
        td: input(type='text', name='login', ng-model='user.login',
          required, ng-class='{validate: doValidate}')
      tr
        td Пароль:
        td: input(type='password', name='password', ng-model='user.password',
          required, ng-class='{validate: doValidate}')
    div.buttons
      a.button.signIn(ng-click='signIn()') Войти
      a.button.signUp(href='/signUp') Присоединиться
  else
    div.menu
      a.button(href='/') Главная
      a.button(href='/picture/add') Добавить картину
      a.button.signOut(ng-click='signOut()') Выйти
