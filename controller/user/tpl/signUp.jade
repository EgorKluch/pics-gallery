extends ../../main/tpl/block/base

block title
  | Регистрация

block content
  p Все поля являются обязательными для заполнения.
  form(ng-controller='SignUpCtrl', name='signUpForm', novalidate)
    table#signUpForm
      tr
        td.label Логин:
        td
          div.error(ng-target='login', ng-error='required')
            | Введите логин, который вы будете использовать для авторизации
          input(type='text', name='login', ng-model='user.login', required,
            ng-class='{validate: doValidate}')
      tr
        td.label Имя:
        td
          div.error(ng-target='name', ng-error='required') Представьтесь, как вас зовут
          input(type='text', name='name', ng-model='user.name', required,
            ng-class='{validate: doValidate}')
      tr
        td.label Пароль:
        td
          div.error(ng-target='password', ng-error='required')
            | Введите пароль, который вы будете использовать для авторизации
          input(type='password', name='password', ng-model='user.password', required,
            ng-change='checkRepeatePassword', ng-class='{validate: doValidate}')
      tr
        td.label Повторите пароль:
        td
          div.error(ng-target='repeatPassword', ng-error='repeatPassword')
            | Введенные пароли не совпадают. Пожалуйста, попробуйте снова
          div.error(ng-target='repeatPassword', ng-error='required')
            | Подтвердите пароль
          input(type='password', name='repeatPassword', ng-model='user.repeatPassword', required,
            ng-change='checkRepeatePassword', ng-class='{validate:doValidate}')
      tr
        td.label Эл. почта:
        td
          div.error(ng-target='email', ng-error='required')
            | Введите почту, к которой будет привязан ваш аккаунт
          input.email(type='email', name='email', ng-model='user.email', required,
            ng-class='{validate:doValidate}')
      tr
        td
        td: div.last
          a.button.signUp(ng-click='signUp()') Присоединиться
